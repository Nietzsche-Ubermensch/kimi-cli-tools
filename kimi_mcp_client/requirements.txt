# Kimi MCP Client Requirements
# Python 3.8+

# Core dependencies
aiohttp>=3.8.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# MCP server dependencies
# (servers are loaded via npx, no Python deps needed)

# Optional enhancements
rich>=13.0.0        # Better CLI output
pytest>=7.0.0       # Testing
pytest-asyncio>=0.21.0  # Async testing
